Load and follow the profile "$ARGUMENTS":

Read `skills/profiles/$ARGUMENTS/SKILL.md` and apply its instructions immediately. The profile will list which component skills to load — read and apply each of them as well.

If the file does not exist, list available profiles by reading the `skills/profiles/` directory.
