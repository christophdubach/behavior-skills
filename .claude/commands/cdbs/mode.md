Load and follow the mode skill "$ARGUMENTS":

Read `skills/modes/$ARGUMENTS/SKILL.md` and apply its instructions immediately.

If the file does not exist, list available modes by reading the `skills/modes/` directory.
