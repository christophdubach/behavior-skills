Load and follow the persona skill "$ARGUMENTS":

Read `skills/personas/$ARGUMENTS/SKILL.md` and apply its instructions immediately.

If the file does not exist, list available personas by reading the `skills/personas/` directory.
