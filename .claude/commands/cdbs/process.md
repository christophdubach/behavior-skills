Load and follow the process skill "$ARGUMENTS":

Read `skills/processes/$ARGUMENTS/SKILL.md` and apply its instructions immediately.

If the file does not exist, list available processes by reading the `skills/processes/` directory.
