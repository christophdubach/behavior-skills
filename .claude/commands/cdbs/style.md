Load and follow the style skill "$ARGUMENTS":

Read `skills/styles/$ARGUMENTS/SKILL.md` and apply its instructions immediately.

If the file does not exist, list available styles by reading the `skills/styles/` directory.
