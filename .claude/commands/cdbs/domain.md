Load and follow the domain skill "$ARGUMENTS":

Read `skills/domains/$ARGUMENTS/SKILL.md` and apply its instructions immediately.

If the file does not exist, list available domains by reading the `skills/domains/` directory.
